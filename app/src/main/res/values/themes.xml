<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ElectricityBillApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary_variant</item>
        <!-- Action bar / toolbar background -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_variant</item>
        <item name="colorAccent">@color/accent</item>
        <!-- Material Design 2 overrides (compatible with your dependencies) -->
        <item name="colorSecondary">@color/accent</item>
        <item name="android:colorBackground">@color/background</item>
    </style>

</resources>